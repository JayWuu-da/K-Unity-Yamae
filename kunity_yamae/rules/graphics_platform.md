# Unity Graphics Platform Rule Card

- Treat graphics/import work as platform-specific.
- Compare Android, iOS, and PC import overrides before recommending changes.
- Check texture compression format, max texture size, mipmaps, read/write, crunch, alpha handling, sprite atlases, mesh compression, shader variants, and render pipeline compatibility.
- Do not assume a desktop-visible asset is acceptable on mobile.
